let fib =
  fix fib x -> let x1 = x - 1 in
               let x2 = x - 2 in
               if x1 = 0 then 1 else
               if x2 = 0 then 1 else
               fib x1 + fib x2
let main =
  print (fib 30)
