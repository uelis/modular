let mul = \y ->
    fix mul x ->
      \a -> if0 x then a else
            let x' = intadd(x,-1) in
            let a' = intadd(a, y) in
            mul x' a'            
let main =
    print (mul 5 7 0)
